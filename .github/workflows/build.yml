name: Java CI with Gradle

on:
  push:
    branches: [ main, dev ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Set up JDK 18
        uses: actions/setup-java@v2
        with:
          java-version: '18'
          distribution: 'adopt'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Print Secret1
        run: echo "The secret is ${{ secrets.DOCKER_USERNAME }}"

      - name: Print Secret2
        run: echo "The secret is ${{ secrets.DOCKER_PASSWORD }}"

      - name: Print Secret3
        run: echo "The secret is ${{ secrets.DOCKER_REPO }}"

      - name: Build Docker image
        run: docker build -t ${{ secrets.DOCKER_USERNAME }}/userCycleManagement:latest .

      - name: Push Docker image
        run: docker push ${{ secrets.DOCKER_USERNAME }}/userCycleManagement:latest
